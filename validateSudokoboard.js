function validateSudokuboard(sudoboardString) {


    //check if length is 16
    if (sudoboardString.length !== 16) return;

    //check string contains no letters

    if (!1234567890.test(sudoboardString)) {
        return;
    }

    //check if it's partially filled with no errors

    //Assign new row to a new substring

    //Assign each column to a new substring

    //Assign each box to new substrings

    //check if partially filled an no errors

    //check if rows are valid

    //check if columns are valid

    // return 'helt utfylt, ingen feil';
}